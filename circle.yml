dependencies:
  override:
   - android-22
   - build-tools-22.0.1
   - extra-android-support
   - extra-google-m2repository
   - extra-android-m2repository

test:
  pre:
    - chmod +x gradlew
  override:
    - ./gradlew cleam assemble